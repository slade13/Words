<p id="notice"><%= notice %></p>

<table class="table table-hover">
  <thead>
  <tr>
    <th>English name</th>
    <th>Polish name</th>
  </tr>
  </thead>

  <tbody>
  <% @words.each do |word| %>
      <tr>
        <td><%= word.english_name %></td>
        <td><%= word.polish_name %></td>
      </tr>
  <% end %>
  </tbody>
</table>